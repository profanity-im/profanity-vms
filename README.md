Profanity VM Test Installs
==========================

Used for testing installation of Profanity on various distributions.

The VMs are created using Vagrant.

To run the install test:

    vagrant up

To remove the VM:

    vagrant destroy

To list all running VMs:

    VBoxManage list runningvms


